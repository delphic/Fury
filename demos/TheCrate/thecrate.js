// Render a Crate!
// Testing Fury's Scene, Shader, Mesh and Material Classes 
// Compare to Arbitary Shader demo which just uses the GL Facade (renderer)

// Init Fury 
Fury.init("fury");

// Create Crate, add to Scene

// Set off Render (single time initially, extend to loop with controls)